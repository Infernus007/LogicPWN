---
title: Logging
description: API documentation for the Logging module in LogicPwn framework
category: Logging
sidebar:
  order: 45
---

import { Code, Aside, Steps, Tabs, TabItem } from '@astrojs/starlight/components';

**Category:** Logging
**Navigation:** [API Reference](../)

API documentation for the `logicpwn.core.logging` module.

## Import

```python
import logicpwn.core.logging
# or
from logicpwn.core.logging import *
```


## Quick Examples

### Basic Usage

```python
from logicpwn.core.exploit_engine import load_exploit_chain_from_file, run_exploit_chain
import requests

# Load and execute a simple exploit chain
session = requests.Session()
chain = load_exploit_chain_from_file("examples/simple_exploit_corrected.yaml")
results = run_exploit_chain(session, chain)

# Analyze results
for result in results:
    print(f"Step: {result.step_name}")
    print(f"Status: {result.status}")
    print(f"Time: {result.execution_time:.2f}s")
```

### Real-World IDOR Testing

```python
from logicpwn.core.access import detect_idor_flaws
from logicpwn.core.auth import AuthConfig, authenticate_session

# Setup authentication
auth_config = AuthConfig(
    url="https://api.example.com/login",
    credentials={"username": "user", "password": "pass"},
    success_indicators=["access_token"]
)

session = authenticate_session(auth_config)

# Test for IDOR vulnerabilities
results = detect_idor_flaws(
    session,
    "https://api.example.com/users/{id}/profile",
    test_ids=["1", "2", "3", "admin", "999"],
    success_indicators=["user_data", "profile"],
    failure_indicators=["unauthorized", "403"]
)
```



## Quick Examples

### Basic Usage

```python
from logicpwn.core.exploit_engine import load_exploit_chain_from_file, run_exploit_chain
import requests

# Load and execute a simple exploit chain
session = requests.Session()
chain = load_exploit_chain_from_file("examples/simple_exploit_corrected.yaml")
results = run_exploit_chain(session, chain)

# Analyze results
for result in results:
    print(f"Step: {result.step_name}")
    print(f"Status: {result.status}")
    print(f"Time: {result.execution_time:.2f}s")
```

### Real-World IDOR Testing

```python
from logicpwn.core.access import detect_idor_flaws
from logicpwn.core.auth import AuthConfig, authenticate_session

# Setup authentication
auth_config = AuthConfig(
    url="https://api.example.com/login",
    credentials={"username": "user", "password": "pass"},
    success_indicators=["access_token"]
)

session = authenticate_session(auth_config)

# Test for IDOR vulnerabilities
results = detect_idor_flaws(
    session,
    "https://api.example.com/users/{id}/profile",
    test_ids=["1", "2", "3", "admin", "999"],
    success_indicators=["user_data", "profile"],
    failure_indicators=["unauthorized", "403"]
)
```



## Quick Examples

### Basic Usage

```python
from logicpwn.core.exploit_engine import load_exploit_chain_from_file, run_exploit_chain
import requests

# Load and execute a simple exploit chain
session = requests.Session()
chain = load_exploit_chain_from_file("examples/simple_exploit_corrected.yaml")
results = run_exploit_chain(session, chain)

# Analyze results
for result in results:
    print(f"Step: {result.step_name}")
    print(f"Status: {result.status}")
    print(f"Time: {result.execution_time:.2f}s")
```

### Real-World IDOR Testing

```python
from logicpwn.core.access import detect_idor_flaws
from logicpwn.core.auth import AuthConfig, authenticate_session

# Setup authentication
auth_config = AuthConfig(
    url="https://api.example.com/login",
    credentials={"username": "user", "password": "pass"},
    success_indicators=["access_token"]
)

session = authenticate_session(auth_config)

# Test for IDOR vulnerabilities
results = detect_idor_flaws(
    session,
    "https://api.example.com/users/{id}/profile",
    test_ids=["1", "2", "3", "admin", "999"],
    success_indicators=["user_data", "profile"],
    failure_indicators=["unauthorized", "403"]
)
```



## Quick Examples

### Basic Usage

```python
from logicpwn.core.exploit_engine import load_exploit_chain_from_file, run_exploit_chain
import requests

# Load and execute a simple exploit chain
session = requests.Session()
chain = load_exploit_chain_from_file("examples/simple_exploit_corrected.yaml")
results = run_exploit_chain(session, chain)

# Analyze results
for result in results:
    print(f"Step: {result.step_name}")
    print(f"Status: {result.status}")
    print(f"Time: {result.execution_time:.2f}s")
```

### Real-World IDOR Testing

```python
from logicpwn.core.access import detect_idor_flaws
from logicpwn.core.auth import AuthConfig, authenticate_session

# Setup authentication
auth_config = AuthConfig(
    url="https://api.example.com/login",
    credentials={"username": "user", "password": "pass"},
    success_indicators=["access_token"]
)

session = authenticate_session(auth_config)

# Test for IDOR vulnerabilities
results = detect_idor_flaws(
    session,
    "https://api.example.com/users/{id}/profile",
    test_ids=["1", "2", "3", "admin", "999"],
    success_indicators=["user_data", "profile"],
    failure_indicators=["unauthorized", "403"]
)
```



## Quick Examples

### Basic Usage

```python
from logicpwn.core.exploit_engine import load_exploit_chain_from_file, run_exploit_chain
import requests

# Load and execute a simple exploit chain
session = requests.Session()
chain = load_exploit_chain_from_file("examples/simple_exploit_corrected.yaml")
results = run_exploit_chain(session, chain)

# Analyze results
for result in results:
    print(f"Step: {result.step_name}")
    print(f"Status: {result.status}")
    print(f"Time: {result.execution_time:.2f}s")
```

### Real-World IDOR Testing

```python
from logicpwn.core.access import detect_idor_flaws
from logicpwn.core.auth import AuthConfig, authenticate_session

# Setup authentication
auth_config = AuthConfig(
    url="https://api.example.com/login",
    credentials={"username": "user", "password": "pass"},
    success_indicators=["access_token"]
)

session = authenticate_session(auth_config)

# Test for IDOR vulnerabilities
results = detect_idor_flaws(
    session,
    "https://api.example.com/users/{id}/profile",
    test_ids=["1", "2", "3", "admin", "999"],
    success_indicators=["user_data", "profile"],
    failure_indicators=["unauthorized", "403"]
)
```


## Classes

:::note[Available Classes]
This module provides 4 class(es) for logging.
:::

### ErrorPattern

<Tabs>
<TabItem label="Overview">

```python
class ErrorPattern:
    """
    Represents a detected error pattern
    """
```

Represents a detected error pattern.

</TabItem>
<TabItem label="Constructor">

```python
def __init__(self, pattern_type: str, error_type: str, frequency: int, recommendation: str, first_occurrence: float, last_occurrence: float) -> None
```

Initialize a new instance of `ErrorPattern`.

</TabItem>
</Tabs>


---

### ErrorTracker

<Tabs>
<TabItem label="Overview">

```python
class ErrorTracker:
    """
    Track error frequency and patterns for better monitoring
    """
```

Track error frequency and patterns for better monitoring.

</TabItem>
<TabItem label="Constructor">

```python
def __init__(self, window_size: int = 3600, pattern_threshold: int = 5)
```

Initialize a new instance of `ErrorTracker`.

</TabItem>
</Tabs>

#### Methods

##### clear_pattern_notifications

:::note[Method]
Instance method
:::

```python
def clear_pattern_notifications(self)
```

Clear pattern notifications to allow re-detection.

##### get_error_statistics

:::note[Method]
Instance method
:::

```python
def get_error_statistics(self) -> dict[str, typing.Any]
```

Get comprehensive error statistics.

##### track_error

:::note[Method]
Instance method
:::

```python
def track_error(self, error_type: str, error_message: str, context: Optional[dict] = None) -> Optional[logicpwn.core.logging.logger.ErrorPattern]
```

Track error occurrence and detect patterns.


---

### LogicPwnLogger

<Tabs>
<TabItem label="Overview">

```python
class LogicPwnLogger:
    """
    Centralized logger for LogicPwn with sensitive data redaction and error tracking
    """
```

Centralized logger for LogicPwn with sensitive data redaction and error tracking.

</TabItem>
<TabItem label="Constructor">

```python
def __init__(self, name: str = 'logicpwn')
```

Initialize a new instance of `LogicPwnLogger`.

</TabItem>
</Tabs>

#### Methods

##### clear_error_pattern_notifications

:::note[Method]
Instance method
:::

```python
def clear_error_pattern_notifications(self)
```

Clear error pattern notifications to allow re-detection.

##### get_error_statistics

:::note[Method]
Instance method
:::

```python
def get_error_statistics(self) -> dict[str, typing.Any]
```

Get error statistics from tracker.

##### log_debug

:::note[Method]
Instance method
:::

```python
def log_debug(self, message: str, data: Optional[dict] = None)
```

Documentation for `log_debug` is not available.

##### log_error

:::note[Method]
Instance method
:::

```python
def log_error(self, error: Exception, context: Optional[dict] = None)
```

Documentation for `log_error` is not available.

##### log_info

:::note[Method]
Instance method
:::

```python
def log_info(self, message: str, data: Optional[dict] = None)
```

Documentation for `log_info` is not available.

##### log_request

:::note[Method]
Instance method
:::

```python
def log_request(self, method: str, url: str, headers: Optional[dict] = None, params: Optional[dict] = None, body: Optional[Any] = None, timeout: Optional[int] = None)
```

Documentation for `log_request` is not available.

##### log_response

:::note[Method]
Instance method
:::

```python
def log_response(self, status_code: int, headers: Optional[dict] = None, body: Optional[Any] = None, response_time: Optional[float] = None)
```

Documentation for `log_response` is not available.

##### log_warning

:::note[Method]
Instance method
:::

```python
def log_warning(self, message: str, data: Optional[dict] = None)
```

Documentation for `log_warning` is not available.


---

### SensitiveDataRedactor

<Tabs>
<TabItem label="Overview">

```python
class SensitiveDataRedactor:
    """
    Handles redaction of sensitive data from logs
    """
```

Handles redaction of sensitive data from logs.

</TabItem>
<TabItem label="Constructor">

```python
def __init__(self)
```

Initialize a new instance of `SensitiveDataRedactor`.

</TabItem>
</Tabs>

#### Methods

##### redact_form_data

:::note[Method]
Instance method
:::

```python
def redact_form_data(self, data: dict[str, typing.Any]) -> dict[str, typing.Any]
```

Documentation for `redact_form_data` is not available.

##### redact_headers

:::note[Method]
Instance method
:::

```python
def redact_headers(self, headers: dict[str, str]) -> dict[str, str]
```

Documentation for `redact_headers` is not available.

##### redact_json_body

:::note[Method]
Instance method
:::

```python
def redact_json_body(self, body: Union[str, dict, list]) -> str
```

Documentation for `redact_json_body` is not available.

##### redact_string_body

:::note[Method]
Instance method
:::

```python
def redact_string_body(self, body: str) -> str
```

Documentation for `redact_string_body` is not available.

##### redact_url_params

:::note[Method]
Instance method
:::

```python
def redact_url_params(self, url: str) -> str
```

Documentation for `redact_url_params` is not available.


---

## Functions

:::note[Available Functions]
This module provides 6 function(s) for direct use.
:::

### log_debug

:::note[Function]
Module function
:::

```python
def log_debug(message: str, data: Optional[dict] = None)
```

Documentation for `log_debug` is not available.

### log_error

:::note[Function]
Module function
:::

```python
def log_error(error: Exception, context: Optional[dict] = None)
```

Documentation for `log_error` is not available.

### log_info

:::note[Function]
Module function
:::

```python
def log_info(message: str, data: Optional[dict] = None)
```

Documentation for `log_info` is not available.

### log_request

:::note[Function]
Module function
:::

```python
def log_request(method: str, url: str, headers: Optional[dict] = None, params: Optional[dict] = None, body: Optional[Any] = None, timeout: Optional[int] = None)
```

Documentation for `log_request` is not available.

### log_response

:::note[Function]
Module function
:::

```python
def log_response(status_code: int, headers: Optional[dict] = None, body: Optional[Any] = None, response_time: Optional[float] = None)
```

Documentation for `log_response` is not available.

### log_warning

:::note[Function]
Module function
:::

```python
def log_warning(message: str, data: Optional[dict] = None)
```

Documentation for `log_warning` is not available.


## Related Modules

:::tip[Logging Modules]
Explore other modules in the Logging category:

- **[Logging › Logger](./logging/logger)** - Main logger implementation
- **[Logging › Redactor](./logging/redactor)** - Sensitive data redaction
:::
